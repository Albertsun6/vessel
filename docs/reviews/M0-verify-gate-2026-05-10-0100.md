# M0 Verify Gate — 2026-05-10 01:00

> 5 项必做（plan §0-meta-lite + ADR-014）。任一失败 → escalation。
> **结果**：✅ **5/5 全过**，M0 验收。

## ✅ Gate 1: Finding 闭环

| 类型 | 数量 | 处理 |
|---|---|---|
| BLOCKER | 4 | 全部 accepted + fixed |
| MAJOR | 9 (重复扣去) | 1 fixed (SIGTERM) / 4 deferred-with-owner+date |
| MINOR | 14 | defer 到对应 milestone |

详见 [Phase 3 arbiter](M0-review-p3-arbiter-2026-05-10-0100.md)。

## ✅ Gate 2: 修复落地

| BLOCKER | 修复路径 | 验证命令 |
|---|---|---|
| 1 migration 隔离 | `migrations-memory/0001_m0_sessions.sql` + session-store.ts MIGRATIONS_DIR + MEMORY_SCHEMA_VERSION=1 | `sqlite3 memory.db "select name from sqlite_master where type='table'"` 仅 sessions/intents/skill_invocations |
| 2 trace redaction | `observability/trace-redactor.ts` (新) + trace-writer.ts wire | `payloadSummary({...})` masks sk-ant / AWS / email / 私人路径 |
| 3 4KB spillover | trace-writer payloadFitsInline check + spill .stdout + summary | `payloadFitsInline(8KB)`=false; .stdout 文件路径 push 到 artifact_refs |
| 4 file mode | session-store mkdir 0o700 + chmod db/wal/shm 0o600；trace-writer dir 0o700 + file 0o600 | `stat -f '%Lp'` traces/ = 700, *.json = 600, memory.db* = 600 |
| MAJOR SIGTERM | vessel-core.ts onShutdown 双信号 | `kill -TERM` 也 graceful 退出 |

## ✅ Gate 3: 回归测试

```
pnpm --filter @vessel/backend exec tsc --noEmit  # exit 0
pnpm --filter @vessel/shared test                # 123 tests pass
M0 acceptance:
  echo hi → "echoed: echo hi"
  sqlite3 → 1 session / 1 intent / 1 skill_invocation status=success
  SIGINT → 3ms exit
  trace-redaction-spec §7 C-3 → grep -q "user_prompt" returns 1 (no match) ✓
```

## ✅ Gate 4: 链接完整性 + Doc 一致性

- `migrations-memory/`、`observability/trace-redactor.ts` 新文件路径一致
- ADR-006 §3 编号约定保持（0004=workflow_state still reserved for M1C-A）
- ADR-014 / ADR-017 review workflow 调用 cursor 异质评审 1 次成功

## ✅ Gate 5: 调研引用

M0 是落地里程碑（实施 0A/0B 已 accepted ADRs），**非新决策** → 不触发 DAR。

## 🚫 Escalation triggers

无：
- ❌ 4 软触发（architect decision-required / disagree-with-evidence / Verify Gate fail / milestone closeout）
- ❌ 4 硬触发：
  - secrets：trace-redaction-spec §7 C-3 通过；redactor 单测 sk-ant/email/私人路径全脱敏
  - license：M0 无新依赖（zod 0B 已加）
  - CVE：无
  - 破坏性数据迁移：BLOCKER 1 已修，harness.db 不被污染

## 决策

✅ **M0 验收通过**。可进 **M0.5**（Coding Driver adapter + capability-coding manifest，按用户指令守边界）。

## debate-review log

```json
{"date":"2026-05-10","planFile":"/Users/yongqian/Desktop/Vessel/docs/reviews/M0-*","totalClaims":21,"accepted":13,"partial":4,"rejected":0,"hung":0,"biggestInsight":"4-way unanimous catch on migration 编号冲突 (BLOCKER 1) — harness-store glob `/^\\d{4}_*\\.sql$/` 在与 session-store 共目录下吞跨边界 SQL；ADR-006 §3 已为 0004 预留 workflow_state 但实施时被 M0 抢走。制度性教训：跨 runner 共享 directory 必有 namespace 隔离","biggestMistake":"我把 0004_m0_sessions.sql 放进 migrations/ 目录就跑通 M0 acceptance，没读 ADR-006 §3 编号清单 + 没察觉 harness-store 会 glob 同目录；4/4 reviewer 一致抓到才意识到","newPrinciplesAdded":0,"newRisksAdded":0,"reviewerSkippedQuestions":[],"counterChallenges":["risk-officer 独家抓 4KB throw + file mode — detail lens 互补 architect/pragmatist 高层 lens"],"contract":"M0 4-way review (cursor + 3 Claude)","mechVersion":"v2-lite"}
```
