# Phase 3 Arbitration — v0A.1 Completion Sprint Review

- **Date**: 2026-05-10 00:30
- **Inputs**: cursor cross-reviewer + Claude combined verdict
- **Total findings**: 14 项（**1 BLOCKER + 9 MAJOR + 4 MINOR**）

> **关键发现**：cursor 找出 **真 BLOCKER**（B1 Trace 三套互斥定义）—— Claude combined 我自己只判 MAJOR（"双 schema 维护负担"）。又一次实证：**cursor 在"集成层 / 实施落地"角度比 Claude lens 更敏锐**。

---

## 仲裁矩阵

| ID | 主张 | Source | 仲裁 | 落地 |
|---|---|---|---|---|
| **cursor B1** | Trace 三套互斥定义（§5 UUID + §5.5 OTEL hex + §5.6 W3C TRACEPARENT）→ 0B-8 实施时按哪份写不清 | cursor | ✅ **BLOCKER 必修** | FRAMEWORK §5 重写成单一 M0 契约：OTEL 命名（32-hex / 16-hex / W3C TRACEPARENT）；§5.5/5.6 仍保留作 prior art 引用；§5.7 删除"推荐 SDK"（按 cursor M5 路径 1）|
| **cursor M1** | ML worker 协议在 FRAMEWORK §4.2 vs ADR-012 §5/§6 互相打架 | cursor | ✅ accepted | ADR-012 §5 删 "JSON-RPC"，§6 引用 FRAMEWORK §4.2 为权威；字段名同步 |
| **cursor M2** | Soul 改 4 sibling，但 ROADMAP M2-Soul-4/-6 仍旧单文件 + personality 字段 | cursor | ✅ accepted | ROADMAP M2-Soul-4 改 3 template 目录；M2-Soul-6 acceptance 改 identity.name + values + guardrails |
| **cursor M3** | AgentResult / ArtifactSchema 用 optional 表达状态，无法 enforce 不变量 | cursor | ✅ accepted | 改 discriminated union（按 cursor 建议） |
| **cursor M4** | 权限 pathAllowlist 没 canonicalization / symlink escape 规则 | cursor | ✅ accepted（4 类硬触发 #5 secrets 临近）| FRAMEWORK §2.3 加 hard rule：realpath canonicalize + 禁止 symlink 逃出 instance root |
| **cursor M5** | OTEL SDK 引入 vs ROADMAP "不引 OTEL 库" 矛盾（recency bias 风险）| cursor | ✅ accepted（**路径 1：M0 自实现 + OTEL 命名 alias，不引 SDK**） | FRAMEWORK §5.7 删 "推荐 SDK"；ROADMAP §11.2 保持"不引 OpenTelemetry 库" |
| Claude M-A1 | A7 Trace 双 schema 维护负担 | Claude | ⚠️ subsumed by cursor B1 | 跟 B1 一起修（M0 直接走 OTEL 命名，单一 schema） |
| Claude M-P1 | A6 端口 11435/11436/11437 硬编码 | Claude | ✅ accepted | FRAMEWORK §4.1 改为"默认端口 + 可配置 + 端口冲突 fallback" |
| Claude M-R1 | A6 HTTP loopback 暴露生命周期管理新风险（僵尸进程 / 认证 / 优雅退出） | Claude | ✅ accepted | FRAMEWORK §4.4 加 worker `/shutdown` endpoint + heartbeat-based zombie detection；考虑 Unix domain socket fallback |
| Claude M-A2 | A1 4 sibling 跟 ADR-005 + ADR-013 联动（vessel init UX / .example 模板）| Claude | ✅ accepted | ADR-005 增订段 + ROADMAP M2-Soul 加 4 sibling 协调 |
| cursor m1 | Intent attachment base64 没大小上限 | cursor | ✅ accepted | FRAMEWORK §8 IntentSchema 加 attachment max size + > N MB 走 artifact_refs |
| cursor m2 | sqlite-vec 在 ADR-012 §2 表格分类混淆 | cursor | ✅ accepted | ADR-012 §2 拆"ML inference worker" vs "local vector storage" |
| Claude m-P1 | ROADMAP §11.5 "milestone 实施时回看" 跟 ADR-014 脱节 | Claude | ⚠️ partial | ADR-014 加一句"每 milestone 启动前回看 IDEAS/ROADMAP §11.5"；不加新流程 |
| Claude m-R1 | HTTP 端口审计（11434 Ollama / 11435+ Vessel） | Claude | ✅ accepted | §4.1 加注 |

**Lens 5 finding**: cursor 自查"OTEL SDK 引入 = recency bias"（M5）+ 我自查"v0A.1 是不是又一次过度工程化"（m-A3）—— 两个 reviewer 都识别出同一倾向，**真 collective blindspot 检测有效**。

## 统计

- ✅ accepted: **12**
- ⚠️ partial / subsumed: 2
- 🚫 rejected: 0
- 🟡 hung: 0
- 🚨 escalation: 0（M4 path canonicalization 临近 4 类硬触发 #5，但按 owner E2 log-not-block policy 处理）

---

## 立即修（5 项关键，BLOCKER + 关键 MAJOR）

按 v5.4 lite 精神，立即修 BLOCKER + 4 个最关键 MAJOR；剩余推到 0B 一起改：

1. ✅ **B1 Trace schema 单一化**：FRAMEWORK §5 重写主 schema 为 OTEL 命名（M0 直接走）；§5.7 删 SDK 推荐
2. ✅ **M1 ADR-012 §5/§6 同步 HTTP loopback**：删 stdio JSON-RPC；引用 FRAMEWORK §4.2 为权威
3. ✅ **M3 AgentResult / Artifact discriminated union**：FRAMEWORK §2.1 + §8
4. ✅ **M4 path canonicalization rule**：FRAMEWORK §2.3 + 加进 R-XX RISKS
5. ✅ **M2 ROADMAP M2-Soul 同步 4 sibling**

剩余 7 项（M-P1 端口 / M-R1 HTTP lifecycle / M-A2 ADR-005 联动 / m1 attachment size / m2 ADR-012 表格分类 / m-P1 ADR-014 / m-R1 端口审计）推到 0B 一起批量改。

---

## 异质性证明（v0A.1 第 4 次实证）

cursor 4-way 评审第 4 次（0-meta-lite dogfood / 0-pre / 0A / 现在 v0A.1）抓 Claude 集体盲区：
- **B1（真 BLOCKER）**：Trace 三套互斥 — Claude 只判 MAJOR
- **M3 discriminated union** — Claude 完全 missed
- **M4 path canonicalization** — risk-officer lens 应该抓但没抓

Claude 互补找了 cursor 没看到的（M-P1 端口硬编码 / M-R1 HTTP lifecycle / M-A2 ADR-005 联动）—— 真互补。

**核心教训**：v0A.1 完善 sprint 提供了"prior art 选型"角度的改进，但 **实施落地细节（schema 一致性 / TypeScript discriminated union / 安全 canonicalization）需要 cursor lens 才能抓**。两者都不可缺。
