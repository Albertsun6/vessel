# Phase 2 React Verdict — vessel-pragmatist

- **Date**: 2026-05-09 21:25
- **Reviewing**: architect-p1 + risk-officer-p1 verdicts
- **Self-findings (P1)**: B-P1 / B-P2 + 4 MAJOR + 3 MINOR

---

## React on architect verdict

| Finding | 4 档 | 反应 |
|---|---|---|
| **B-A1** Coding Driver 在 5/6 接口归属 | **disagree-with-evidence** | 这是过早 abstraction。v0.1 只有一个 driver（CC CLI），何必先定义 Driver 接口契约？等真正加第 2 个 driver（Cursor/Codex 在 v1+）时再决定 5 vs 6 接口。**Pragmatist 反提案**：v0.1 不动 5 接口，CodingDriver 就是个普通 module（不实现任何 5 接口）。等 v1+ 真要扩多 CLI 时再做接口抽象 + 修订 ARCHITECTURE。理由：YAGNI + 不为不存在的需求做接口设计。Architect 在 P2 react 提的"折中：抽 Driver interface 但不动 cli-runner 内部"——同意比 P1 的"必须 0A 决定 5 vs 6"灵活得多。 |
| B-A2 Boot 三层 index.ts 重构 | refine | 同意是真问题，但**时机**：0-pre 不必先做 index.ts 重构 plan。M0 第一个 Acceptance 是"vessel-core CLI"——这时自然要拆 boot 三层（至少进程级 + Session）。Instance 级（soul.md 注入）等到 M2-Soul 再做。EVA_TO_VESSEL_MAPPING 加一行 `index.ts → 拆三层 boot` 落到 M0/M2-Soul，但 0-pre 阶段不阻塞。 |
| B-A3 5 接口存放位置 | agree | 0A FRAMEWORK 必须锁定。 |
| M-A1 Soul Spec 作用范围演进 | agree | |
| M-A2 ML worker 在 5 接口归属 | agree | |
| M-A3 telemetry vs trace.ts 共存 | agree | |
| M-A4 Workflow 应在 control_plane/ 子目录 | agree | 跟 ARCHITECTURE §3 5 层架构一致更可读。 |
| m-A1 ARCHITECTURE 搬迁前移 | agree-with-evidence | 实用建议——0-meta-lite 第一步就该搬，让 reviewer 找得到。 |
| m-A2 canonical 顺序 | agree | |

## React on risk-officer verdict

| Finding | 4 档 | 反应 |
|---|---|---|
| **B-R1** 🚨 Secrets 扫描前置 | agree | 这是非常关键。Pragmatist 视角补充：可以用现成工具 `gitleaks` / `trufflehog` 一行命令解决，不增工程量。 |
| **B-R2** ML worker 启动失败 fallback | agree | |
| **B-R3** 🚨 Trace 脱敏 | agree | Pragmatist 视角："脱敏"用现成库（`fast-redact` / `pino-redact`）即可，不必自研。 |
| M-R1 iOS bundle id | agree-with-evidence | Pragmatist 视角更严重——这其实涉及 **Apple Developer 流程**（不只是配置改名）。0-pre 应该完整盘点 iOS 配置。 |
| M-R2 secrets 自动化 | agree | |
| M-R3 drop 检测 SOP | agree | |
| M-R4 iOS Local Network 权限 | agree | |
| m-R1 RISKS Verification 列 | agree | |
| m-R2 inbox 措辞 | agree | |

---

## Phase 2 增量观察

跟 architect 在 B-A1（Coding Driver 接口归属）有分歧 —— 这是 YAGNI vs 架构纯度的经典权衡。Phase 3 仲裁需要 owner 拍板（属于 decision-required）。

跟 risk-officer 完全一致——所有 secrets / 数据迁移 / 启动失败 finding 都该 accept。
