# Phase 3 Arbitration — v5.4 Self-Review

- **Date**: 2026-05-09 21:40
- **Author**: Claude (debate-review SKILL)
- **Inputs**: 3 × Phase 1 verdicts + 3 × Phase 2 react verdicts
- **Total findings**: 27（8 BLOCKER + 12 MAJOR + 7 MINOR）

---

## 仲裁矩阵（4 档）

| ID | 主张概要 | 仲裁 | 落地动作 |
|---|---|---|---|
| **B-A1** | Coding Driver 5/6 接口归属 | 🟡 **hung（owner 决策）** | 不阻塞 0-pre；0A FRAMEWORK 之前必须 owner 拍板（Phase 2 architect 提的折中"抽 Driver interface 但不动 cli-runner 内部"是当前最优候选） |
| B-A2 | Boot 三层 index.ts 重构路径 | ✅ accepted | EVA_TO_VESSEL_MAPPING 加 `index.ts → 拆三层 boot` 行，落到 M0（进程级 + Session 级）/ M2-Soul（Instance 级）。0-pre 不阻塞 |
| B-A3 | 5 接口存放位置 | ✅ accepted | 0A FRAMEWORK 锁定：跨端类型 → `shared/`；5 接口 → `backend/src/interfaces/`；接口可单向 import shared/ 类型 |
| **B-P1** | 不重构 cli-runner，加 façade | ⚠️ **partial（融合）** | 采用 architect P2 react 的折中：M0.5 抽**最小 Driver interface**（`submit(spec) → artifact`），cli-runner.ts **实现**这个 interface，**不动** cli-runner 内部逻辑。Risk-officer 增订：façade/wrapper 之外加单元测试覆盖 5 集成挑战边界 |
| B-P2 | 0-pre 工作量低估 | ✅ accepted | 0-pre Acceptance 第 1 条改 "覆盖**核心模块**（M0–M1C 路径上的文件）"；frontend/ios 非核心标 `coverage: not-yet-mapped` 进 RISKS |
| **B-R1** | 🚨 Secrets 扫描前置 | ✅ accepted（**escalation-required**） | 加 0-meta-lite 第 6 项 Acceptance："`gitleaks detect --no-git -v` 扫 Eva 仓库无 high-severity finding"；推荐工具锁 `gitleaks`；ADR-014 §「硬触发」第 5 条加具体工具 |
| B-R2 | ML worker 启动失败 fallback | ✅ accepted | ADR-012 Consequences 加 ④ 条（首次启动失败模式 + 检测策略）；M1C-B Acceptance 加 "`vessel-core --health` 报具体原因"；R-11 缓解策略加 5 类首次启动失败归因 |
| **B-R3** | 🚨 Trace 脱敏白/黑名单 | ✅ accepted（**escalation-required**） | 0A FRAMEWORK 段必须加"Trace 脱敏规则"小节（白名单字段 + 黑名单匹配 + > 4KB 切 artifact_refs + 文件 mode 600）；新加 ADR-016-trace-redaction 或扩 ADR-014；M0 Acceptance 加 trace 文件 mode 600 检查 |
| M-A1 | Soul Spec 作用范围演进 | ✅ accepted | RISKS 新增 R-12："Soul Spec v0.1 仅 cli-runner 注入；非 cli-runner Skill（MCP / EchoSkill / Voice）不带 soul prompt——v1+ 决定是否扩展"；CONCEPTS §1.2 加"Soul Spec 作用范围"段（0A 时） |
| M-A2 | ML worker 在 5 接口归属 | ✅ accepted | 0A FRAMEWORK 锁定：embedding-client = Memory 接口的 internal helper；asr-client / tts-client = Voice Capability 的 internal helper（不是单独接口） |
| M-A3 | telemetry-store.ts vs observability/trace.ts 共存 | ✅ accepted | EVA_TO_VESSEL_MAPPING 加明确动作 "in-place 改 telemetry-store.ts 为 12 字段"；observability/trace.ts 定位 = "client 接口 + 子进程环境变量传播协议"，不取代 telemetry-store |
| M-A4 | Workflow 在 control_plane/ 子目录 | ✅ accepted | 目录结构改 `packages/backend/src/control_plane/{orchestrator,workflow,session,hitl}.ts`，跟 ARCHITECTURE §3 5 层架构一致 |
| M-P1 | Eva protocol.ts 字段先盘点 | ✅ accepted | 0-pre EVA_INVENTORY 加专门一节列 protocol.ts 现有所有消息类型 + 字段；EVA_TO_VESSEL_MAPPING 加 "protocol.ts 字段复用清单" |
| **M-P2** | 11 ADR Tier 1/2/3 | ⚠️ **partial（融合）** | 采用 architect refine + risk-officer disagree 的折中：**所有 ADR 都正式写**（risk lens 决策路径完整性），但篇幅可短（Tier 2/3 = 5-15 行 vs Tier 1 = 50+ 行）；**Phase 0 调研只在 Tier 1（DAR 触发条件命中）才跑**；Tier 划分按"是否触及 DAR"非工作量 |
| M-P3 | vessel-researcher YAGNI | ✅ accepted | 不动（plan 已保留为可接受）|
| M-P4 | CLI 命令格式 `pnpm vessel-core` | ✅ accepted | 0A FRAMEWORK 锁定 CLI 入口实现（推荐 backend `package.json` `bin` 字段 + root `package.json` `scripts.vessel-core` 转发）；0B Acceptance 加 `pnpm vessel-core --version` 检查 |
| M-R1 | iOS bundle id 改名风险 | ✅ accepted | R-09 缓解策略细化 checklist；0-pre EVA_INVENTORY 列出当前 Eva iOS 完整配置（bundle id / display name / Team ID / cert / profile / icon resources） |
| M-R2 | secrets 自动化（gitleaks pre-commit） | ✅ accepted | 0-meta-lite 加 pre-commit hook 调 `gitleaks`（极小工程量）；ADR-014 §「硬触发」第 5 条引用此 hook |
| M-R3 | 破坏性数据迁移 SOP | ✅ accepted | ADR-006 增订段加自动 schema diff 工具（dry-run 后 diff 旧 vs 新 schema，报 drop column / drop table / drop index）；0B Acceptance 加 "dry-run 输出无 drop 操作，或 drop 有 owner approval 文件" |
| M-R4 | iOS Local Network 权限 fallback | ✅ accepted | M2-iOS Acceptance 加 "Local Network 权限被拒时 UI 显示具体原因 + 手填 IP 入口可见"；新增 R-13 "iOS Local Network 权限拒绝率风险" |
| m-A1 | ARCHITECTURE 搬迁前移 | ✅ accepted | 0-meta-lite 阶段就把 ARCHITECTURE.md / CONCEPTS.md 搬到 `docs/architecture/`（实际**还没做** —— 见 Verify Gate #4 失败）|
| m-A2 | 5 接口 canonical 顺序 | ✅ accepted | 0A FRAMEWORK 锁定字母序：Agent / App / Memory / Skill / Tool |
| m-P1 | frontend 沿用模糊 | ✅ accepted | EVA_TO_VESSEL_MAPPING 加 frontend 重构动作："M1A 之前保留 Eva UI；M2 之后看用户需求决定是否重设计" |
| m-P2 | 0-meta-lite Acceptance 第 5 条 N/A 项处理 | ✅ accepted | 改为"5 项 Verify Gate **适用项**手动通过；N/A 项明确标注"|
| m-P3 | 工程方法论原则数不一致 | ✅ accepted | grep 修一致（"8 条" / "9 条" → "10 条"）|
| m-R1 | RISKS Verification 列 | ✅ accepted | RISKS 表加 Verification 列（每条 RISKS 给可执行检查命令）|
| m-R2 | inbox 措辞 | ✅ accepted | 第一次提到 `instance/inbox/` 加注 "(v5.4 lite: 手动跑，未自动化)"|

---

## 统计

- ✅ accepted: **22**
- ⚠️ partial: **2**（B-P1 + M-P2，都是 P2 react 已经融合的）
- 🚫 rejected: **0**
- 🟡 hung（owner 决策）: **1**（B-A1 Coding Driver 接口归属）
- 🚨 escalation-required（4 类硬触发）: **2**（B-R1 secrets + B-R3 trace 脱敏）

## 修复落地策略（v5.4 lite 精神）

**不立即写 v5.5** —— 否则陷入 v5.5/5.6 循环，违反评审 AI 第六轮的"不再扩展评审系统"原则。

落地分两类：

### A. 0-meta-lite 阶段必须立即处理（escalation + 关键准备）

1. ✅ 把 ARCHITECTURE.md / CONCEPTS.md 搬到 `docs/architecture/`（m-A1）—— 立刻做（Verify Gate #4 当前失败）
2. ✅ 加 0-meta-lite 第 6 项 Acceptance："gitleaks 扫 Eva 无 high-severity finding"（B-R1）
3. ✅ 写 secrets 扫描 + pre-commit hook 准备（B-R1, M-R2）—— 命令记到 docs/reviews/README.md
4. 🚨 escalation 通知 owner：B-A1 / B-R1 / B-R3（**写到 inbox**）
5. ✅ ADR-014 增订段：补 gitleaks 工具（M-R2）

### B. 0-pre / 0A 阶段处理（不阻塞 0-meta-lite 收尾）

剩余 19 项 finding 落到对应阶段：
- **0-pre EVA_INVENTORY** 任务：M-P1（protocol 字段盘点）、M-R1（iOS 完整配置）
- **0-pre EVA_TO_VESSEL_MAPPING** 任务：B-A2（index.ts 拆 boot）、M-A3（telemetry vs trace）、m-P1（frontend）
- **0A FRAMEWORK** 任务：B-A3、M-A2、M-A4、M-P4、m-A2、B-R3 trace 脱敏小节
- **0A ADR 写作**：B-A1（owner 拍板后写 ADR-016 或扩 ADR）、M-A1（CONCEPTS §1.2 加 Soul 作用范围）、M-P2（Tier 划分）
- **0A RISKS**：R-12（Soul 作用范围）、R-13（iOS Local Network 权限）、m-R1（Verification 列）、B-P2（覆盖核心模块）、B-R2（M1C-B 启动失败）
- **0B**：M-P4（CLI 入口）、M-R3（schema diff）、m-P2（Verify Gate N/A）
- **M0/M0.5**：B-P1 partial（最小 Driver interface 实现）、B-R3（trace mode 600）、M-A4（control_plane/ 目录）
- **M1C-B**：B-R2（health check）

## 反向挑战（给下一轮评审者）

1. **3 个 reviewer 都是同模型（Claude）共有偏见的可能**：B-R1/B-R3 一致接受可能是 over-cautious 隐私敏感。但隐私问题不能 over-cautious，保留。
2. **没人挑战 v5.4 整体方向（D' 路线）**：说明前 6 轮已经收敛，或者集体盲区——下次 milestone closeout 时**手动跑外部 AI 终审**做异质兜底。
3. **B-P1 / B-A1 在 P2 已自动融合**：这是 B' 流程的成功示例——不同 lens 在 cross-pollinate 阶段就收敛，不必每次都到 Phase 3 author 仲裁。
4. **dogfood 暴露的最大价值**：让 plan v5.4 的"M0.5 重构 cli-runner"得到精确化（最小 Driver interface 而非完整重构）—— 这是 v5.4 plan 自评最大的具体收益。

## 给 owner 的 inbox 通知（escalation）

3 项需要 owner 决策：

1. **B-A1 Coding Driver 接口归属**（decision-required）：5 接口扩到 6 接口（加 Driver） vs CodingDriver 复用 Tool 接口 vs Phase 2 折中"抽 Driver interface 不动 cli-runner 内部"。**0A 之前必须拍板**。**推荐**：折中方案（Phase 2 architect 提的）。
2. **B-R1 Secrets 扫描**（4 类硬触发）：建议 0-meta-lite 阶段立刻跑 `gitleaks detect` 扫 Eva 仓库；如发现 high-severity，按 ADR-013 决定 git filter-repo 还是放弃 fork。**这是 0-pre 的安全前置**。
3. **B-R3 Trace 脱敏**（4 类硬触发）：0A FRAMEWORK 必须加脱敏白/黑名单段；M0 acceptance 加文件 mode 600 检查。

owner 收到后批准这 3 项，0-meta-lite 即可收尾，进 0-pre。
