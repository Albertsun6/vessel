# M0.5 Phase 3 arbiter verdict — 2026-05-10 01:40

## Phase 1 inputs (4 reviewers)

| Reviewer | Verdict | BLOCKER | MAJOR | MINOR | File |
|---|---|---|---|---|---|
| vessel-architect | PASS-WITH-FIXES | 0 | 7 | 4 | [`p1-architect`](M0.5-review-p1-architect-2026-05-10-0140.md) |
| vessel-pragmatist | PASS-WITH-FIXES | 0 | 3 | 3 | [`p1-pragmatist`](M0.5-review-p1-pragmatist-2026-05-10-0140.md) |
| vessel-risk-officer | PASS-WITH-FIXES | 0 (1 critical-MAJOR) | 4 | — | [`p1-risk-officer`](M0.5-review-p1-risk-officer-2026-05-10-0140.md) |
| cursor cross | PASS-WITH-FIXES | 0 | 3 | 2 | [`cross`](M0.5-review-cross-2026-05-10-0140.md) |

## 异质性确认 ✅ 第 7 次 cursor + Claude 互补

- **convergent (≥2 reviewers)** finding 都是真问题；非 padding
- **risk-officer 独家**：cli-runner.ts:116 already-aborted edge case (cursor + architect 都没看到)
- **architect 独家**：FRAMEWORK.md §2.2 SkillContext schema 滞后于代码（5 字段 vs 4）
- **pragmatist 独家**：redactor SUFFIXES 14 条暴增，false-positive 风险

## 4 档分类矩阵

### ✅ 接受（已 fix in this commit）

| Finding | Source | 修复 | 验证 |
|---|---|---|---|
| **MAJOR-1** vessel-core hard-exit setTimeout(5000) vs cli-runner KILL_GRACE_MS=5000 race → 父进程先 exit detached child group 孤儿写盘 | risk-officer R-M0.5-1 + cursor MAJOR-2 + architect MAJOR-3 | vessel-core fallback 5000 → **6000** (cli-runner 5s 内必须先 SIGKILL) | comment annotated; integration test 仍通过 |
| **MAJOR-2** redactor object 递归只 mask 命中 SUFFIX 的 leaf；CC schema 加新字段会绕过（如 `.input.new_field`） | risk-officer R-M0.5-3 + cursor MAJOR-1 | `redactValue` 加 `forceMask` 参数；SUFFIX 命中后所有子树 leaves 强制 mask | 真 CC 跑 fibonacci stdout grep 0 leaks |
| **MAJOR-3** cli-runner.ts:116 `addEventListener` 不处理 already-aborted 信号；早 cancel race 漏过 | risk-officer R-M0.5-2 | 加 `if (p.signal?.aborted) onAbort()` after listener 注册 | 1 行 fix |
| **MAJOR-4** orchestrator echo prefix foot-gun: `echo this homework` 走 echo 路径而非 coding | architect MAJOR-6 | 改 dispatch：default = coding；显式 `--skill=echo\|coding` flag override | 真 CC 默认 coding；test runIntent 显式 driver inject |

### ⚠️ 部分接受（defer）

| Finding | Source | Defer 决策 | Owner+截止 |
|---|---|---|---|
| **MAJOR** TOCTOU mkdir/realpath race (symlink swap window) | architect MAJOR-2 | 单用户可信机器 + workspace mode 0700，攻击面极窄；加 lstat 用 O_NOFOLLOW 是 v1+ hardening | M1B 接 MCP 时一并加 |
| **MAJOR** redactor SUFFIXES 14 条 over-redaction（毁掉 trace 可读性） | pragmatist F-P3 | M0.5 接受 false-positive 大于 false-negative（用户 review focus 明令"不会泄露 prompt/stdout"为最重）；trace 可读性 v1+ 重新平衡 | v1+ trace UI |
| **MAJOR** SkillContext 加 abortSignal 但 FRAMEWORK.md §2.2 仍 4 字段 | architect MAJOR-5 | M1A 写设计文档同步时一起更（5 接口契约 frozen 是 paper，git 还没 commit） | M1A |
| **MAJOR** cli-runner-driver 第二层 realpath 是过度防御（caller 是同进程） | pragmatist F-P2 | 防御 valuable when CodingDriver 后期被 IPC / WS 调用（M1A WS 后）；保留 5 行 | — |
| **MAJOR** stdout line-by-line redact OOM risk for large tasks | pragmatist | M0.5 任务规模小不爆；M2-Voice 大流式 worker 时改 streaming redact | M2-Voice |
| **MINOR** ADR-016 said "≤5 lines" cli-runner diff，actual 12 | architect MINOR-1 | M0.5 closeout 改 ADR-016 备注：`≤15 行` (additive only) | now |
| **MINOR** capability-loader 36 行属"超 boundary 但 ROI 正" | pragmatist accepted | 接受作 retrospective | — |
| **MINOR** manifest.permissionScope 仅声明 (M1B enforce) | architect MAJOR-7 | M1B 时 enforce | M1B |

### 🚫 反驳

无（4/4 reviewer findings 都站得住脚或合理 defer）。

### 🟡 挂起

无。

## Phase 3 行动汇总

- ✅ 修 4 MAJOR (5s 预算 / redactor 子树 / abort already-aborted / echo prefix)
- ⚠️ Defer 5 MAJOR + 2 MINOR (含 owner+截止)
- ✅ tsc clean / shared 123 tests / coding-driver integration 全过
- ✅ Real CC 写 fibonacci.py → exit 0 + 输出 55 + stdout 0 prompt/code leak
- ✅ ADR-016 微调 (单独行 in this verdict commit)

## 评审 calibration

- 4/4 reviewer 全 PASS-WITH-FIXES 收敛 → M0.5 守界 + 无致命 BLOCKER
- 用户列的 4 项 review focus：#1 PASS（cli-runner diff 12 行 minimal）/ #2 PASS-WITH-DEFER（realpath 正确，TOCTOU 加固延后）/ #3 PASS（5s race 已修）/ #4 PASS（redactor 子树 force-mask 后 byte-level audit 0 leak）

## 决策

**M0.5 验收通过**（PASS）。可以进 Verify Gate → M1A。
