# Phase 2 React Verdict — vessel-architect

- **Date**: 2026-05-09 21:25
- **Reviewing**: pragmatist-p1 + risk-officer-p1 verdicts
- **Self-findings (P1)**: B-A1 / B-A2 / B-A3 + 4 MAJOR + 2 MINOR

---

## React on pragmatist verdict

| Finding | 4 档 | 反应 |
|---|---|---|
| **B-P1** 不重构 cli-runner，加 thin wrapper façade | **disagree-with-evidence** | 架构 lens 认为：Coding Driver 边界必须显式抽象。façade 在 v0.1 看起来省事，但 v1+ 加 Cursor/Codex 时还要再做一次（"先 façade 后接口"两次工作量）。**建议折中**：M0.5 直接抽 Driver interface（最小版：`submit(spec) → artifact`），cli-runner.ts 实现这个 interface，不动 cli-runner 内部逻辑。这样既得到接口又不破坏 Eva 已踩过的坑。Pragmatist 的"包装层"和我的"接口"差距其实不大——包装层 = 接口 + 实现绑死。 |
| B-P2 0-pre 工作量低估 | agree | 同意。"覆盖核心模块"比"全部 src"务实。 |
| M-P1 protocol 字段先盘点 | agree | 同意。补到 0-pre EVA_INVENTORY 里。 |
| M-P2 11 ADR Tier 1/2/3 | refine | 思路对，但分级标准应改"是否触及 DAR 触发条件"而非"工作量"。Tier 1 = DAR 命中（必须正式 + Phase 0）；Tier 2 = DAR 未命中但需要决策 trace；Tier 3 = 决策已固化（合并到其他 ADR 或 README）。 |
| M-P3 vessel-researcher YAGNI | agree | Pragmatist 自己也接受保留。 |
| M-P4 CLI 命令格式 | agree | 0A 必须锁定。 |
| m-P1 frontend 沿用模糊 | agree | |
| m-P2 N/A 项处理 | agree | |
| m-P3 原则数不一致 | agree | |

## React on risk-officer verdict

| Finding | 4 档 | 反应 |
|---|---|---|
| **B-R1** 🚨 Secrets 扫描前置 | agree | 这是关键。Architect lens 看：secrets 扫描应作为 0-meta-lite 完成判定的额外一项（在 dogfood 之前，这样 dogfood 自评通过后立即可以安全地 fork-rename）。 |
| **B-R2** ML worker 启动失败 fallback | agree | 我（architect）的 P1 verdict 漏了这个失败模式。同意补 ADR-012 ④。 |
| **B-R3** 🚨 Trace 脱敏白/黑名单 | agree-with-evidence | 跟我 P1 的 B-A1 相关——trace 经过 Driver 层时同样要脱敏。Architect 视角补充：Trace schema（OpenTelemetry-lite 12 字段）的 `payload` 字段应该有强类型 schema（pydantic / Zod），而非自由 JSON——schema 强制脱敏字段。 |
| M-R1 iOS bundle id | agree | 不是 architect lens 但合理。 |
| M-R2 secrets 自动化 | agree | |
| M-R3 drop 检测 SOP | agree-with-evidence | Schema 演进 ADR-006 应该加自动化 diff 工具（结合 ADR-014 硬触发第 8 条）。 |
| M-R4 iOS Local Network 权限 | agree | |
| m-R1 RISKS Verification 列 | agree | |
| m-R2 inbox 措辞 | agree | |

---

## Phase 2 增量观察

跟 pragmatist 在 B-P1 上有真分歧（架构纯度 vs 工程实用），需要 Phase 3 仲裁。其他 23 条 finding 三方 lens 基本一致——B' 流程的多 lens 价值在 B-P1/B-A1 这种 trade-off 议题上体现。

Risk-officer 提的 B-R1 / B-R3 是 4 类硬触发命中，按 ADR-014 **必须** escalation 到 owner。
