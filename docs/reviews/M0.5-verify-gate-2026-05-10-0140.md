# M0.5 Verify Gate — 2026-05-10 01:40

> 5 项必做（plan §0-meta-lite + ADR-014）。
> **结果**：✅ **5/5 全过**

## ✅ Gate 1: Finding 闭环

| 类型 | 数量 | 处理 |
|---|---|---|
| BLOCKER | 0 | — |
| MAJOR (合并) | 9 | 4 fixed / 5 deferred-with-owner+date |
| MINOR | 9 | defer / accept-as-is |

[Phase 3 arbiter](M0.5-review-p3-arbiter-2026-05-10-0140.md) 4 档分类完整。

## ✅ Gate 2: 修复落地

| Fix | 路径 | 验证 |
|---|---|---|
| 5s 预算 race | vessel-core.ts:74 setTimeout(5000)→6000 | comment annotated |
| redactor 子树 force-mask | trace-redactor.ts:redactValue + forceMask param | real CC stdout grep 0 leaks |
| cli-runner already-aborted | cli-runner.ts add `if (p.signal?.aborted) onAbort()` | 1 行 fix |
| echo prefix foot-gun | orchestrator resolveSkill default=coding + `--skill=echo\|coding` flag | CLI --help 文档更新 |

## ✅ Gate 3: 回归测试

```
pnpm --filter @vessel/backend exec tsc --noEmit  # exit 0
pnpm --filter @vessel/shared test                # 123 tests pass
pnpm --filter @vessel/backend test:coding-driver # 13 assertions pass
real CC: pnpm vessel-core "写 fibonacci ... print fib(10)" → fibonacci.py + exit 0 + python3 ... = 55
trace stdout grep "递归|fibonacci|fib\\(n\\)" → empty (no leaks)
```

## ✅ Gate 4: 链接完整性 + Doc 一致性

- M0.5 新文件路径全部存在 (verify in arbiter doc grep)
- ADR-016 path C 实施一致 (cli-runner.ts 净增 12 行 additive only)
- capability-coding/manifest.yaml validates against AppManifestSchema

## ✅ Gate 5: 调研引用

M0.5 是落地里程碑（实施 0A/0B 已 accepted ADRs：ADR-016 path C / ADR-003 仅 CC / ADR-013 rename），**非新决策** → 不触发 DAR yes/no 检查表。

## 🚫 Escalation triggers

无：
- ❌ 4 软触发（无 decision-required / 无 disagree-with-evidence / Verify Gate pass / closeout 已跑外部 cursor）
- ❌ 4 硬触发：
  - secrets：trace-redaction 子树 force-mask + real CC stdout grep 0 leak
  - license：M0.5 引入 `yaml@^2.5.0` (MIT / `THIRD_PARTY_LICENSE_LOG.md` 已记)
  - CVE：无
  - 破坏性数据迁移：M0.5 不动 schema

## 决策

✅ **M0.5 验收通过**。可以进 **M1A**（多入口 + session 共享 — Eva Hono+WS+Web 接到 Vessel session_id 模型，按用户严守边界指令仅做 plan §M1A 范围）。

## debate-review log

```json
{"date":"2026-05-10","planFile":"/Users/yongqian/Desktop/Vessel/docs/reviews/M0.5-*","totalClaims":17,"accepted":4,"partial":7,"rejected":0,"hung":0,"biggestInsight":"M0.5 4-way review 0 BLOCKER 但 4 convergent MAJOR — 风险已从 BLOCKER 级降到 deep-correctness 级；ADR-016 path C adapter 模式 + Eva 复用 + redactor 子树 force-mask 三件凑齐让 vessel-core 真能跑代码不漏 prompt","biggestMistake":"redactor 第一版只 path-blacklist mask leaf；CC schema evolution 可绕过 → risk-officer 抓出，需 forceMask 子树递归。教训：redaction 设计必须默认 over-mask 而非 under-mask","newPrinciplesAdded":0,"newRisksAdded":0,"reviewerSkippedQuestions":[],"counterChallenges":["pragmatist 提议 redactor SUFFIXES 14→8 砍 false-positive 但 risk-officer 提议子树 force-mask 反而扩大 mask 覆盖；矛盾经 arbiter 仲裁定为：M0.5 接受 false-positive 大于 false-negative，trace UI 可读性 v1+ 重新平衡"],"contract":"M0.5 4-way review (cursor + 3 Claude)","mechVersion":"v2-lite"}
```
