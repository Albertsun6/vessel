{{systemPrompt}}

# Stage: {{stage}}

## Goal

{{stageGoal}}

## Inputs

{{#if upstreamArtifacts.length}}
Upstream artifacts available:
{{#each upstreamArtifacts}}
- `{{this.kind}}` at `{{this.key}}`
{{/each}}
{{else}}
(no upstream artifacts)
{{/if}}

## Memory hits (lessons learned from past runs)

{{#if memoryHits.length}}
{{#each memoryHits}}
### {{@index}}. {{this.failurePattern}}

**Fix**: {{this.fix}}
**Source**: {{this.source}} | shipCount: {{this.shipCount}}

{{/each}}
{{else}}
(no relevant lessons)
{{/if}}

## Output format

Produce a markdown artifact for stage `{{stage}}`. Stay ≤ 1800 words.
Use Bullet > Paragraph. Be specific about unknowns and irreversible decisions.

## Hard limits

- Token budget: ≤ 1800 words
- No filler / no apology / no "as an AI"
