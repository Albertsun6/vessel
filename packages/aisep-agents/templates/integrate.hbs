{{systemPrompt}}

# Stage: integrate — merge + tag + deploy (or simulate)

## Goal

Produce a structured `integration-log.json` that records what would be
done to merge the verified+reviewed patch into the target branch. AISEP
does NOT auto-execute git/deploy commands in v0; this stage produces the
*plan* for the human (or future automated integrate executor) to apply.

This is the **G-i / IOC anchor** equivalent — the last machine gate
before the change reaches the target system.

## Inputs

{{#if upstreamArtifactsWithContent.length}}
{{#each upstreamArtifactsWithContent}}
### Upstream `{{this.ref.kind}}` @ `{{this.ref.key}}`

```
{{this.contentPreview}}
{{#if this.truncated}}
[... {{this.truncatedBytes}} bytes truncated]
{{/if}}
```

{{/each}}
{{else}}
(read `review-verdict.json` + `patch.diff` + `verify-report.json` from cwd)
{{/if}}

## Memory hits

{{#each memoryHits}}
- **{{this.failurePattern}}** → {{this.fix}}
{{/each}}

## Output format (MANDATORY — structured JSON)

```json
{
  "ready_to_integrate": true | false,
  "target_branch": "dev",
  "merge_strategy": "squash | merge-commit | rebase",
  "commit_message": "<imperative-mood subject line>\n\n<body>",
  "tag": "<vX.Y.Z-Mz or null>",
  "ci_required_checks": ["test", "dep-cruiser:check", "..."],
  "migration_safe": true | false,
  "migration_notes": "<what would need to run before merge, or null>",
  "rollback_path": "<exact command(s) to revert if integrate goes wrong>",
  "deferred_followups": [
    { "owner": "user | maintainer", "task": "<one-line>" }
  ],
  "blockers": [
    { "id": "B1", "description": "<what stops integrate from proceeding>" }
  ]
}
```

## Hard limits

- `ready_to_integrate=false` if ANY of the following:
  - `review-verdict.json` verdict was `revise_required`
  - `verify-report.json` shows ANY failed build/lint/unit/integration/e2e/security
  - Any blocker in `blockers[]` is non-empty
- `migration_safe=true` requires explicit checklist:
  - no schema breaking change
  - no removed public export
  - no dependency-cruiser regression
- `rollback_path` is MANDATORY and must be a runnable command, not a
  description. ("git revert <SHA>" is fine; "manually undo the changes"
  is not.)
- After the JSON, output ONE-paragraph human summary (≤ 100 words) for
  the reviewer to skim. Place it AFTER the JSON, not inside.
